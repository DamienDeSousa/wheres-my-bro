import { AvailableBros } from '@/components/AvailableBros'
import { render, screen } from '@testing-library/react'
import '@testing-library/jest-dom'

describe('TownInput', () => {
  test('', () => {
    // mock session
    //  connectDB()
    // UserAccount.findOne
    render(<AvailableBros />)
  })
})
